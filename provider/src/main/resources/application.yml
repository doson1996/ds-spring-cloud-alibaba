server:
  port: 8082

spring:
  application:
    name: provider
  cloud:
    nacos:
      discovery:
        server-addr: ds.com:8848
        #  namespace: ds-spring-cloud-alibaba
        #  group: dev
        #  username: nacos
        #  password: nacos
        namespace: ds-spring-cloud-alibaba
        group: dev
        username: ds
        password: ds


management:
  endpoints:
    web:
      exposure:
        include: '*'